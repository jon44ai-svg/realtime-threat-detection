# Ultralytics resolves `path` from the process cwd (project root), not this file.
path: data/raw
train: train/images
val: valid/images
test: test/images

nc: 3
names:
  0: blunt_object
  1: gun
  2: knife
