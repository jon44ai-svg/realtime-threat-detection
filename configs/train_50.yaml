# Colab / NVIDIA GPU defaults (override locally with --device cpu)
# Local AMD Vega / CPU machines should pass --device cpu.
model: yolov8n.pt
data: configs/data.yaml
epochs: 50
imgsz: 640
batch: 16
device: 0
workers: 2
project: runs/detect
name: train_50
exist_ok: true
patience: 50
seed: 0
save: true
plots: true
