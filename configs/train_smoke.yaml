# Short smoke train (~5% of data, 2 epochs) to verify the pipeline.
model: yolov8n.pt
data: configs/data.yaml
epochs: 2
imgsz: 640
batch: 4
fraction: 0.05
device: cpu
workers: 2
project: runs/detect
name: train_smoke
exist_ok: true
patience: 2
seed: 0
save: true
plots: true
