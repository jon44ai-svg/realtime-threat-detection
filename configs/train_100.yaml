# Colab / NVIDIA GPU defaults (override locally with --device cpu)
model: yolov8n.pt
data: configs/data.yaml
epochs: 100
imgsz: 640
batch: 16
device: 0
workers: 2
project: runs/detect
name: train_100
exist_ok: true
patience: 100
seed: 0
save: true
plots: true
